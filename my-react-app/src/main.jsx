import React from "react";  //Imports the React library
import ReactDOM from "react-dom/client";  //Imports ReactDOM, which connects React to the browser DOM
import App from "./App.jsx";  //Imports the main App component

ReactDOM.createRoot(document.getElementById("root")).render(<App />);

//document (Finds the <div id="root"></div> in index.html)
//ReactDOM (Creates a React root)







